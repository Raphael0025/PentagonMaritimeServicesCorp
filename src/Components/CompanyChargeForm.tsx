'use client';

import { useEffect, useState } from 'react';

interface IProps{
    onClose: () => void;
    id: string;
}

export default function CompanyChargeForm({ onClose, id } : IProps){

    return(
        <>
        </>
    )
}
